@page "/"
@using LoginAttemptDemo.Models
@inject Data.LookupService lookup

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

@code {
    List<UserModel> users;

    protected override async void OnInitialized()
    {
        //base.OnInitialized();
        users = await lookup.UserLookup("", "");
        var g = "";
    }
}